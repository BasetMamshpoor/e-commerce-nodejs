# اصلاحات و تغییرات موردنیاز پروژه

بعضی از موارد زیر قبلا درست شده اند.

---

# 1. تغییر نوع شناسه‌ها (ID)

* تمام شناسه‌های اصلی (Primary Key) که در حال حاضر از نوع `string` هستند به `integer` تغییر داده شوند.
* از Auto Increment پیش‌فرض دیتابیس استفاده شود و نیازی به تولید شناسه سفارشی نیست.

---

# 2. بازطراحی کامل ساختار Product و Variant

ساختار فعلی Product و Variant از نظر مفهومی صحیح نیست و باید بازطراحی شود.

## انواع Attribute

Attributeها به سه دسته تقسیم می‌شوند:

### 1. Attributeهای فیلتر

ویژگی‌هایی مانند:

* کشور سازنده
* برند
* نوع سوخت
* جنس
* و هر ویژگی دیگری که صرفاً برای فیلتر کردن محصولات استفاده می‌شود.

این Attributeها هیچ نقشی در ساخت Variant ندارند.

---

### 2. Attributeهای نمایشی

ویژگی‌هایی که فقط در صفحه جزئیات محصول نمایش داده می‌شوند و روی قیمت، موجودی یا تنوع محصول تأثیری ندارند.

---

### 3. Attributeهای تنوع (Variant Attributes)

ویژگی‌هایی مانند:

* رنگ
* سایز
* حافظه
* ظرفیت
* جنس پارچه
* و سایر ویژگی‌هایی که باعث ایجاد تنوع محصول می‌شوند.

فقط این دسته از Attributeها باید در ساخت Variant استفاده شوند.

---

## قیمت پایه و Variantها

هر محصول باید یک **Base Price** داشته باشد که حداقل قیمت محصول است.

اگر محصول هیچ Variant نداشته باشد:

* قیمت نهایی همان Base Price خواهد بود.
* فقط موجودی محصول ثبت می‌شود.

اگر محصول دارای Variant باشد:

ادمین فقط Attributeهای تنوع و مقادیر آن‌ها را انتخاب می‌کند.

برای مثال:

رنگ:

* قرمز
* آبی

سایز:

* S
* XL

جنس:

* کتان
* ابریشم

سپس **Frontend** تمام ترکیب‌های ممکن را به‌صورت خودکار تولید می‌کند، مانند:

* قرمز + S + کتان
* قرمز + S + ابریشم
* قرمز + XL + کتان
* آبی + XL + ابریشم
* ...

ادمین برای هر ترکیب فقط دو مقدار را وارد می‌کند:

* موجودی (Stock)
* مقدار افزایش قیمت (Price Adjustment)

قیمت نهایی هر Variant برابر است با:

**Final Price = Base Price + Price Adjustment**

بنابراین هیچ Variant قیمت مستقل ندارد و فقط مقدار افزایش قیمت نسبت به Base Price را نگهداری می‌کند.

---

## ساختار Variant

ساختار فعلی که شامل `attributeValueIds[]` است صحیح نیست.

ساختار Variant باید براساس ترکیب واقعی Attributeهای تنوع بازطراحی شود و فقط Attributeهای مربوط به تنوع در آن قرار گیرند.

---

## تصاویر محصول

تصویر برای Variant وجود ندارد.

تمام تصاویر متعلق به Product هستند.

هر محصول آرایه‌ای از تصاویر دارد که هر آیتم شامل فایل تصویر و فیلد `isMain` است.

حداقل یک تصویر الزامی است و دقیقاً یکی از تصاویر باید `isMain = true` باشد.

---

## Response محصولات

Response مربوط به Product و Variant باید کاملاً Flat باشد تا Frontend بتواند بدون پردازش اضافی از آن استفاده کند.

---

## تخفیف محصول

تخفیف متعلق به کل محصول است، نه Variant.

ادمین بتواند تخفیف را به دو صورت ثبت کند:

* درصدی
* مبلغ ثابت

این تخفیف روی قیمت نهایی تمام Variantها اعمال می‌شود.

---

# 3. Cart

تمام تغییرات مربوط به Variantها باید در Cart نیز اعمال شوند.

منطق Merge سبد خرید باید بازطراحی شود.

همچنین در مستندات دقیقاً مشخص شود که شناسه یکتای هر آیتم Cart چیست (برای مثال ProductId به همراه شناسه یا ترکیب Variant) تا Frontend و Backend از یک منطق واحد استفاده کنند.

---

# 4. Product Detail

Endpoint دریافت جزئیات محصول با ID نیز اضافه شود.

---

# 5. Product Listing

امکان مرتب‌سازی محصولات بر اساس موارد زیر اضافه شود:

* جدیدترین
* پرفروش‌ترین
* پربازدیدترین
* محبوب‌ترین (براساس میانگین امتیاز کاربران)

---

# 6. Wishlist

در جزئیات هر محصول فیلدی مانند `isWish` یا `isLiked` وجود داشته باشد تا مشخص شود کاربر لاگین‌شده این محصول را قبلاً به Wishlist اضافه کرده است یا خیر.

---

# 7. Related Products

در جزئیات هر محصول دو لیست مجزا برگردانده شود:

* محصولات مرتبط
* محصولاتی که همراه این محصول بیشتر خریداری شده‌اند

همچنین لیست وبلاگ‌های مرتبط با محصول نیز برگردانده شود.

در زمان ایجاد یا ویرایش Blog نیز امکان انتخاب محصولات مرتبط وجود داشته باشد.

---

# 8. Comparison API

ساختار فعلی این API اشتباه است.

تنها یک Endpoint کافی است.

Frontend باید حداقل ۱ و حداکثر ۴ شناسه محصول را از طریق Query ارسال کند.

Backend اطلاعات کامل همان محصولات را برگرداند تا عملیات مقایسه توسط Frontend انجام شود.

تمام Endpointهای افزودن، حذف و خالی کردن لیست مقایسه حذف شوند.

---

# 9. بازطراحی کامل Media

ساختار فعلی Media مناسب نیست.

هر Entity باید مسئول مدیریت تصاویر خود باشد.

مانند:

* Product
* Blog
* Banner
* Brand
* و ...

Media عمومی حذف شود.

---

## تصاویر Responsive

فقط در بخش‌هایی که اندازه تصویر اهمیت دارد، سه نسخه جداگانه دریافت شود:

* Mobile
* Tablet
* Desktop

این الزام برای تمام بخش‌ها نیست و فقط در محل‌های موردنیاز اعمال شود.

---

## تصاویر Brand

Brand نیز باید دارای تصویر باشد.

---

## URL تصاویر

در تمام Responseهای API هرجا تصویری وجود دارد باید URL کامل فایل برگردانده شود.

نباید شناسه داخلی یا مسیر ناقص مانند:

* imageId
* logoUrl
* imageUrl

برگردانده شود.

---

# 10. Comments

در تمام Responseهای مربوط به Comment اطلاعات زیر وجود داشته باشد:

* AuthorId
* AuthorName

همچنین فیلد `isLiked` براساس Token کاربر و با استفاده از `optionalAuthenticate` برگردانده شود.

نمایش کامنت‌ها برای **کاربر و ادمین** باید به‌صورت درختی (Tree) باشد.

در پنل ادمین نیز فیلترهای زیر اضافه شوند:

* نوع
* تعیین وضعیت شده / تعیین وضعیت نشده
* جستجوی محصول

---

# 11. Wishlist

در Endpoint `GET /wishlist` ساختار Product دقیقاً مشابه لیست محصولات باشد و فقط تصویر اصلی محصول (`isMain = true`) برگردانده شود.

---

# 12. Sitemap

در `sitemap.xml` علاوه بر صفحه اصلی، موارد زیر نیز اضافه شوند:

* محصولات فعال
* دسته‌بندی‌ها
* برندهای فعال

---

# 13. Categories

در Response محصولات ساختار Junction حذف شود.

به‌جای آن دسته‌بندی‌ها به‌صورت Flat برگردانده شوند.

---

# 14. Stories

Endpoint جدید Stories ایجاد شود.

هر Story شامل فیلدهای زیر باشد:

* title
* coverImage
* videoUrl
* expiresAt
* nextId
* prevId

هر Story نیز بتواند چند محصول مرتبط داشته باشد.

---

# 15. Landing Page API

یک Endpoint اختصاصی برای صفحه Landing ایجاد شود.

تمام اطلاعات موردنیاز صفحه اصلی از همین Endpoint دریافت شوند، از جمله:

* Bannerها
* Sliderها
* Stories
* Notificationها
* دسته‌بندی‌ها
* آخرین وبلاگ‌ها
* لینک شبکه‌های اجتماعی
* اطلاعات فروشگاه
* برندهای محبوب
* پرفروش‌ترین محصولات
* پیشنهادها براساس بازدید کاربر
* پیشنهادها براساس خریدهای قبلی
* و سایر بخش‌های صفحه اصلی

---

# 16. Newsletter

Endpoint جدید برای عضویت کاربران در خبرنامه ایمیلی ایجاد شود.

---

# 17. Global Search

Endpoint جدید برای جستجوی سراسری در سایت ایجاد شود.

---

# 18. Checkout

ثبت نهایی سفارش فقط برای کاربران با نقش Customer مجاز باشد.

---

# 19. Notification After Order

پس از ثبت موفق سفارش:

* در صورت وجود شماره موبایل، پیامک ارسال شود.
* در غیر این صورت ایمیل ارسال گردد.

---

# 20. پنل مشتری

* قابلیت Avatar برای مشتری حذف شود.
* امکان ثبت درخواست برداشت از کیف پول فراهم شود.
* پس از ثبت درخواست، برای بررسی و تأیید به پنل ادمین ارسال شود.

---

# 21. پنل ادمین

## Ticket

امکان فیلتر تیکت‌ها بر اساس موارد زیر اضافه شود:

* وضعیت
* شماره تیکت
* تاریخ
* دپارتمان
* شناسه کاربر

همچنین تیکت‌هایی که ۵ روز از آخرین پاسخ پشتیبانی آن‌ها گذشته باشد، به‌صورت خودکار بسته شوند.

---

## کاربران

بخش مدیریت کاربران توسعه داده شود و Endpointهای جدیدی برای موارد زیر ایجاد گردد:

* مشاهده تمام سفارشات کاربر
* وضعیت کیف پول
* افزایش موجودی کیف پول
* کاهش موجودی کیف پول
* لیست تراکنش‌ها
* سایر اطلاعات مدیریتی موردنیاز

---

## سفارشات مرجوعی

به لیست مرجوعی فیلترهای کامل اضافه شود.

در Endpoint:

`GET /api/v1/orders/admin/returns`

مبلغ پرداخت‌شده نیز در لیست نمایش داده شود.

همچنین Endpoint زیر اضافه گردد:

`GET /api/v1/orders/admin/returns/:returnId`

که تمام جزئیات مرجوعی شامل موارد زیر را برگرداند:

* اطلاعات کامل سفارش
* آیتم‌های سفارش
* یادداشت مشتری
* اطلاعات مرجوعی
* سایر جزئیات مرتبط

---

## وضعیت سفارش

در Endpoint تغییر وضعیت سفارش، امکان ثبت اطلاعات زیر اضافه شود:

* کد مرسوله
* شماره بسته

---

## Notification ادمین

سیستم اعلان برای ادمین اضافه شود تا رویدادهای مهم از جمله موارد زیر را نمایش دهد:

* سفارش جدید
* درخواست مرجوعی
* درخواست برداشت از کیف پول
* سایر رویدادهای مهم سیستم

---

# نکات مهم

* تمام مستندات پروژه باید همزمان با اعمال این تغییرات به‌روزرسانی شوند.
* تمامی Responseها باید ساختاری استاندارد، Flat و مناسب استفاده در Frontend داشته باشند.
* از ایجاد Endpointهای تکراری و ساختارهای غیرضروری خودداری شود.
* در طراحی نهایی، سادگی استفاده برای Frontend، قابلیت توسعه و مقیاس‌پذیری در اولویت باشد.

